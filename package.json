{
  "name": "sql-insert-editor",
  "displayName": "SQL Insert Editor",
  "version": "0.0.3",
  "description": "Helps in editing SQL insert statements.",
  "publisher": "ReneSaarsoo",
  "author": "Rene Saarsoo <nene@triin.net>",
  "license": "GPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/nene/sql-insert-editor"
  },
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onLanguage:sql"
  ],
  "contributes": {
    "commands": [
      {
        "command": "sql-insert-editor.deleteColumn",
        "title": "Delete column"
      },
      {
        "command": "sql-insert-editor.moveColumnBefore",
        "title": "Move column before"
      },
      {
        "command": "sql-insert-editor.moveColumnAfter",
        "title": "Move column after"
      },
      {
        "command": "sql-insert-editor.sortColumns",
        "title": "Sort columns"
      }
    ],
    "submenus": [
      {
        "id": "sql-insert-editor.submenu",
        "label": "SQL insert editor"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "submenu": "sql-insert-editor.submenu",
          "when": "editorLangId == sql",
          "group": "1_modification"
        }
      ],
      "sql-insert-editor.submenu": [
        {
          "command": "sql-insert-editor.moveColumnBefore",
          "group": "1_move@1"
        },
        {
          "command": "sql-insert-editor.moveColumnAfter",
          "group": "1_move@2"
        },
        {
          "command": "sql-insert-editor.sortColumns",
          "group": "1_move@3"
        },
        {
          "command": "sql-insert-editor.deleteColumn",
          "group": "2_delete"
        }
      ]
    },
    "configuration": {
      "title": "SQL Insert Editor",
      "properties": {
        "sqlInsertEditor.dialect": {
          "type": "string",
          "default": "sqlite",
          "enum": [
            "sqlite",
            "mysql",
            "mariadb",
            "postgresql",
            "bigquery"
          ],
          "enumDescriptions": [
            "SQLite",
            "MySQL",
            "MariaDB",
            "PostgreSQL",
            "BigQuery"
          ],
          "description": "SQL dialect to use when parsing."
        }
      }
    }
  },
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "npm run clean && esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
    "watch": "npm run compile -- --watch",
    "lint": "eslint src --ext ts",
    "test": "vitest",
    "package": "npm run compile && vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.2",
    "dedent": "^1.7.2",
    "esbuild": "^0.28.1",
    "typescript": "^5.5.0",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "sql-parser-cst": "^0.42.1"
  }
}
